<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="com.dbottillo.mtgsearchfree"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:name=".MTGApp"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@drawable/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config"
        android:label="@string/app_name"
        android:theme="@style/MTGSearchTheme"
        tools:ignore="GoogleAppIndexingWarning,UnusedAttribute">

        <activity
            android:name=".ui.HomeActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.cards.CardsActivity"/>

        <activity android:name=".ui.lucky.CardLuckyActivity"
                  android:launchMode="singleTask"
                  android:parentActivityName=".ui.HomeActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.HomeActivity" />
        </activity>

        <activity android:name=".ui.about.AboutActivity"/>

        <activity android:name=".ui.about.ReleaseNoteActivity"/>

        <activity android:name=".ui.search.SearchActivity"/>

        <activity android:name=".ui.decks.deck.DeckActivity"/>

        <activity android:name=".ui.sets.SetPickerActivity" />

        <meta-data
            android:name="com.crashlytics.ApiKey"
            android:value="f2c7b2dc347786aa1e01b4ca437f2f8dd05d59d8"/>

        <service android:name=".model.network.NetworkIntentService"/>

        <service
            android:name=".util.CardMigratorService" android:exported="false"/>

        <receiver
            android:name=".widget.LuckyWidgetProvider"
            android:icon="@drawable/ic_launcher"
            android:label="Lucky Widget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/lucky_widget_info"/>
        </receiver>

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths"/>
        </provider>

    </application>

</manifest>
