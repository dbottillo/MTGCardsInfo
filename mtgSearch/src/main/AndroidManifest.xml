<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dbottillo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />-->

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:name=".base.MTGApp"
        android:theme="@style/MTGSearchTheme">
        <activity
            android:name=".base.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:theme="@style/mtg.NoActionBar"
            android:name=".cards.CardsActivity" />

        <activity android:name=".cards.CardLuckyActivity" />

        <activity
            android:theme="@style/mtg.NoActionBar"
            android:name=".cards.FullScreenImageActivity" />

        <activity
            android:name=".search.SearchActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>

        <activity
            android:theme="@style/mtg.NoActionBar"
            android:name=".saved.SavedActivity" />

        <activity android:name=".lifecounter.LifeCounterActivity" />

        <meta-data
            android:name="com.crashlytics.ApiKey"
            android:value="f2c7b2dc347786aa1e01b4ca437f2f8dd05d59d8" />

        <service android:name=".network.NetworkIntentService" />
    </application>


</manifest>
